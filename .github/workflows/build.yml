name: CAPlayground V6 (Identity Shift)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # 1. EXTRACT
      - name: Extract Source
        run: |
          if [ -f "ios_source.tar.gz" ]; then
            tar -xzf ios_source.tar.gz
          elif [ -d "ios" ]; then
            echo "📂 Using existing 'ios' folder."
          else
            echo "❌ Error: No source found."
            exit 1
          fi

      # 2. CACHE PODS
      - name: Cache CocoaPods
        uses: actions/cache@v3
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-v6-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-v6-

      # 3. INSTALL PODS
      - name: Install Pods
        run: |
          cd ios
          if [ -f "Podfile" ]; then
            pod install || pod install --repo-update
          fi

      # 4. MASTER PATCH (The "Identity Shift")
      - name: Apply Master Patch
        run: |
          cd ios
          echo "🎨 Starting Identity Shift..."

          # A. Fix Median .zip bug
          # We use a loop to be thorough
          find . -name "LEANWebViewController.m" -exec sed -i '' 's/stringByAppendingPathExtension:@"zip"//g' {} +
          find . -name "GNJSBridgeInterface.m" -exec sed -i '' 's/stringByAppendingPathExtension:@"zip"//g' {} +

          # B. VERIFY PATCH (Safety Check)
          # If the bad code is still there, we fail the build immediately.
          if grep -r "stringByAppendingPathExtension:@\"zip\"" .; then
            echo "❌ CRITICAL: The .zip patch failed! Bad code still exists."
            exit 1
          else
            echo "✅ Patch verified: No forced .zip extensions found."
          fi

          # C. Modify Info.plist (Name, ID, Permissions)
          PLIST=$(find . -name "Info.plist" | head -n 1)
          
          if [ -n "$PLIST" ]; then
            echo "   📝 Editing $PLIST..."
            
            # 1. Rename App
            /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName CAPlayground" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :CFBundleDisplayName string CAPlayground" "$PLIST"
            
            # 2. Change Bundle ID (Makes it YOUR app)
            /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.nightvibes3.caplayground" "$PLIST"
            
            # 3. Set Version (1.0.RunNumber)
            BUILD_NUM="${{ github.run_number }}"
            /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUM" "$PLIST"
            /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.0.$BUILD_NUM" "$PLIST"

            # 4. Enable File Sharing
            /usr/libexec/PlistBuddy -c "Add :UIFileSharingEnabled bool true" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Set :UIFileSharingEnabled true" "$PLIST"
            /usr/libexec/PlistBuddy -c "Add :LSSupportsOpeningDocumentsInPlace bool true" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Set :LSSupportsOpeningDocumentsInPlace true" "$PLIST"
            
            echo "   ✅ Identity Shift Complete: com.nightvibes3.caplayground v1.0.$BUILD_NUM"
          fi

      # 5. BUILD (High Performance Mode)
      - name: Build App
        run: |
          cd ios
          WORKSPACE=$(find . -maxdepth 1 -name "*.xcworkspace" | head -n 1)
          if [ -n "$WORKSPACE" ]; then
             BUILD_ARG="-workspace $WORKSPACE"; SCHEME=$(basename "$WORKSPACE" .xcworkspace)
          else
             PROJECT=$(find . -maxdepth 1 -name "*.xcodeproj" | head -n 1)
             BUILD_ARG="-project $PROJECT"; SCHEME=$(basename "$PROJECT" .xcodeproj)
          fi
          
          echo "🚀 Building $SCHEME (Performance Mode)..."
          
          xcodebuild build \
            $BUILD_ARG \
            -scheme "$SCHEME" \
            -sdk iphoneos \
            -configuration Release \
            -derivedDataPath build/ \
            -arch arm64 \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            SWIFT_COMPILATION_MODE=wholemodule \
            GCC_GENERATE_DEBUGGING_SYMBOLS=NO \
            STRIP_INSTALLED_PRODUCT=YES

      # 6. PACKAGE
      - name: Package IPA
        run: |
          cd ios
          mkdir Payload
          APP_PATH=$(find build/Build/Products/Release-iphoneos -name "*.app" | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ Build failed."
            exit 1
          fi
          
          cp -r "$APP_PATH" Payload/

          # Clean Signature for SideStore
          rm -rf "Payload/$(basename "$APP_PATH")/_CodeSignature"
          
          IPA_NAME="CAPlayground_v1.0.${{ github.run_number }}.ipa"
          zip -r -y -X "$IPA_NAME" Payload

          echo "IPA_NAME=$IPA_NAME" >> $GITHUB_ENV

      # 7. UPLOAD
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: CAPlayground-GodTier-App
          path: ios/${{ env.IPA_NAME }}
